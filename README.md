# ariadne
External memory and conversation-state service for Open WebUI — keeps per-project state out of the prompt and injects a small, budgeted context pack each turn, so any chat resumes at a fixed token cost.
