# AGENTS.md

This project keeps its agent working instructions in **[`CLAUDE.md`](CLAUDE.md)**. Read that
file — it is the single entry point (repo layout, hard rules, conventions, do-nots, and the
definition of done for any task). This file exists only so tools that look for `AGENTS.md` find
the same instructions; there is no separate content here, and the two must not diverge.
